# 安全策略与漏洞报告指南

OmniTool 的核心安全准则是：**用户数据全生命周期不出本机，未受信任的插件必须限制在沙盒内运行**。任何破坏这两项基础防线的行为，均被项目组视为最高优先级的安全漏洞。

## 漏洞披露渠道

**严禁在公开的 GitHub Issue、论坛、公开讨论区或社交网络上发布未修复的安全漏洞细节。**

请使用 GitHub 官方的私密漏洞披露渠道：
访问代码仓库的 [Security → Advisories](https://github.com/Okysu/OmniTool/security/advisories/new)，点击「Report a vulnerability」创建私密通告。

在安全报告中，请尽可能详细地提供：

- 漏洞类型与危害边界评估（例如：“非受信插件可逃逸沙盒读取未授权的用户私有文件”）；
- 受影响的具体分支、Tag 版本或代码提交哈希；
- 稳定重现的 PoC（概念验证），建议附带最小化的攻击样本插件源码或特制输入文件；
- 如已有初步的研究结论，请附带你所建议的缓解措施或补丁方向。

维护团队将在 **7 个工作日内**完成报告评估并提供初步处置反馈。漏洞修复确认并完成发布后，我们将在安全公告与更新日志中郑重鸣谢漏洞报告者。

## 支持的版本范围

在进入 1.0.0 正式稳定版本前，项目处于高频迭代阶段，安全维护**仅覆盖当前最新的次版本分支**。

| 软件版本区间 | 安全维护状态 |
| --- | --- |
| 0.5.x | 支持中，优先修复 |
| 0.4.x 及更早版本 | 不再提供安全修复，请升级至最新版本 |

## 漏洞认定边界

### 属于受支持的安全漏洞范畴

- **沙盒穿透与逃逸**：隔离沙盒内的插件代码成功获取主页面的 DOM、Cookie、Local/Indexed 存储，或越权窃取其他插件的数据上下文。
- **能力未授权突破**：插件在未获用户显式授权的情况下，直接调用未声明的宿主原生能力，或非法越权访问非本管道生命周期内的外部文件。
- **非预期网络数据外泄**：插件绕过 CSP 策略或宿主 `host.net` 代理机制直接对外发起 Socket / HTTP 连接；未声明 `net` 权限的插件将任何明文或密文数据发送出用户设备。
- **密态凭据泄露**：插件读取到在宿主 `secret` 服务中托管的 API Key、Token 凭据明文，或将凭据定向发送至用户白名单之外的未受信域名。
- **未授权提权（Privilege Escalation）**：第三方订阅在无感知状态下自动获取了更高的系统能力；动态依赖加载机制绕过了初次安装时审核确认的指纹与版本约束。
- **模型与核心资产篡改**：本地 AI 模型或运行时动态模块的 SHA-256 / SRI 校验可被绕过或伪造。
- **跨站与脚本注入**：插件声明的结构化组件、动态文件名或富文本预览触发了宿主上下文的非预期代码执行（XSS）。

### 不属于本项目的安全漏洞范畴

- 用户在充分理解安全风险的前提下，**主动授权**第三方插件使用 `net` 外联等高敏权限，插件随后按其声明实施的网络通信行为。
- 用户主动在配置面板中注入的个人自定义 CSS 样式规则所引发的样式错乱。
- 依赖于用户操作系统环境已被攻破、本地系统被置入木马，或用户安装了高特权恶意浏览器扩展的场景。
- 上游第三方开源依赖库（如底层 pdf.js、FFmpeg 核心本身）的通用漏洞：建议直接向对应上游基金会或组织反馈；但如果 OmniTool 的集成方式导致该漏洞的利用面被放大，依然欢迎向我们提交分析报告。

## 安全架构白皮书

关于沙盒隔离实现深度、宿主权限代理机制与虚拟私有存储的详细技术实现，请查阅 [系统架构设计白皮书](docs/design/01-architecture.md) 第三章；关于各宿主能力的使用约束，请阅读 [Plugin API 规范手册](docs/design/02-plugin-api.md)。