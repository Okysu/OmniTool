# 更新日志

本项目的所有重要变更均记录于此。

格式规范遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号管理遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。
在 1.0.0 正式版发布前，次版本号（Minor）的变动可能包含向下不兼容的改动；**Plugin API 的不兼容变更均会明确标注**。

每个版本均对应一轮完整的技术迭代，决策背景与测试验证记录可参阅 [`docs/iterations/`](docs/iterations/)。

## [Unreleased]

### 新增

- 采用 [MIT 许可证](LICENSE) 正式开源，并补充完整的 [第三方许可声明](THIRD_PARTY_NOTICES.md)（含 FFmpeg WebAssembly 核心相关的 GPL 合规说明与源码获取指引）。
- 建立社区治理规范：贡献指南、安全漏洞披露策略、行为准则，以及标准化的 Issue 与 Pull Request 模板。
- 新增项目文档索引 [`docs/README.md`](docs/README.md)。
- **AI 扩展工具箱**（[`extensions/`](extensions/README.md)）：随仓库维护、不随应用内置的订阅扩展，连接任意 OpenAI 兼容接口（OpenAI、DeepSeek、通义千问、硅基流动、智谱、Kimi、OpenRouter、本机 Ollama / LM Studio），提供 21 个工具：
  - 文本：翻译（Markdown 保留代码块、SRT / VTT 保留时间轴、i18n JSON 只译值、术语表、双语对照）、长文摘要、润色改写、信息抽取、文本分类、表格逐行处理、会议纪要、带引用的文档问答、自定义提示词。
  - 视觉：目标检测打标（兼容 0–1000 / 0–1 / 像素 / Gemini 坐标，导出 COCO、YOLO、LabelMe）、图片描述、图片 / PDF 转 Markdown、看图问答、图片分类整理、表格 / 图表转 CSV、截图转网页、智能命名。
  - 语音与生成：云端语音转写（本机压缩音轨、长音频分段）、语音合成、图像生成。
  - API Key 由 `secret` 能力保管并绑定接口域名，插件无法读回明文。
- 端到端测试新增 `extensions` 套件：真实订阅、审查授权、录入凭据，并对模拟的 OpenAI 兼容接口运行扩展工具。
- **工作流置顶**：工作流可以像工具一样置顶到侧边栏，打开后是独立的运行页，直接拖入文件或输入文本即可运行（首个步骤接受文本时）。置顶的工作流也出现在命令面板中，所有工作流均可搜索。
- **流程图节点自由摆放**：拖动节点可放到画布任意位置（包括上下排列），位置随工作流保存与导出；连线自动从朝向下一节点的一侧连出；「自动排列」恢复从左到右的布局。

### 变更

- 重塑 README 结构，对齐主流开源工程规范：产品架构、功能矩阵、快速启动、自托管部署、插件开发、安全模型及贡献指南。
- 同一插件在短时间内向同一域名多次使用凭据时，提示合并为一条并显示次数，批量任务不再刷屏。
- **工作流页面改版**：默认只展示「我的工作流」卡片；点击「新建工作流」后填写名称并选择空白或模板。工作流编辑器改为不带侧边栏与顶栏的全屏页面（与插件教程一致），流程图占满高度，参数检查器在选中步骤时浮于画布之上。
- 流程图画布的滚轮平移改为上下左右均可（Shift + 滚轮横向）。

### 修复

- 修复通过导入或订阅安装的插件在刷新页面后丢失的问题（安装记录因响应式代理无法写入 IndexedDB，且错误被静默忽略）。保存失败现在会明确提示。
- 修复安装审查把 `host.net.fetch(...)` 误报为「直接网络调用（将被拦截）」，以及把实际使用的 `secret`、`onnx` 能力判定为未使用的问题。
- 修复对话框内容超出对话框宽度的问题（如订阅安装审查中较长的工具描述与来源地址）：对话框网格列此前会被不换行的文字撑宽。
- 修复拖放区在窄栏中「接受的格式」一行不换行、溢出容器的问题。
- 修复侧边栏在有失效置顶项（插件已卸载）时拖动排序会移动错误条目的问题。

## [0.5.0] - 2026-09-17

### 新增

- **可视化工作流画布**：支持自由平移与缩放的流式编排面板，提供自动拓扑布局、连线插入步骤以及右侧属性检查器；运行时可实时监控各节点状态与处理文件吞吐。保留原有线性列表视图以供自由切换。
- **交互式插件演练场**（`/#/learn`）：内置教学环境，整合原理讲解、代码编辑器与基于实际沙盒的即时运行预览；包含 12 节由浅入深的实战课程与配套练习。
- **面向 Agent / LLM 的上下文文档**：构建阶段自动生成 `/llms.txt` 与 `/llms-full.txt`，萃取插件接口规范、类型定义与标准参考实现。
- **本地模型缓存管理**：在「设置 → 本地模型」中提供权重文件的状态监控、占用分析与一键清理；下载源支持在 Hugging Face 官方源、hf-mirror 镜像源或私有源之间无缝切换，全程受 SHA-256 完整性校验保护。
- 完善 Vercel 静态托管配置 `vercel.json`，内建 Cross-Origin-Opener-Policy 与 Cross-Origin-Embedder-Policy 响应头策略，默认激活浏览器跨源隔离。
- 升级全套产品标识与矢量 Favicon。

### 变更

- 重构任务调度面板为非阻塞的全局浮窗：常规状态下收拢为紧凑胶囊，支持按需展开队列细节与全局拖拽。

### 修复

- 完善音视频工具边界防御：当输入仅含视频画面而缺少音频流时，波形生成、声道提取、语音转文字等工具将直接返回友好提示，避免底层的 FFmpeg 进程抛出非受控异常。
- 修复从主应用上下文路由跳转至教程演练场时沙盒预览未初始化的问题。

## [0.4.0] - 2026-09-17

### 新增

- 内置工具集从 57 款扩充至 95 款：
  - **PDF 体系**：多图与手写签名合成、文本覆写、真·页面涂黑（整页栅格化，防止文字底层泄露）、扫描件图像增强、交互式表单填报、页面自动纠偏倾斜、细粒度权限管控与加密解密、文件结构修复、Web 线性化快速视图优化、电子作品集构建。
  - **图形图像**：多协议条码/二维码生成与识别、位图高精度矢量化（Bitmap to SVG）、SVG 栅格化、直方图分析、图像感知哈希查重、像素级比对、CSS 精灵图合成、色彩置换与滤镜矩阵、表情包排版、扫描拼贴自动切分、接缝裁减（Seam Carving）内容感知缩放、空域降噪。
  - **音视频管道**：色彩校正与画面修复、无损分段与音频剪切、动态音频波形绘制、静态图/动图转视频封装、视频水印压制、字幕全生命周期工具（格式转换、软字幕提取、硬字幕压制、中文字体烧录）。
  - **数据与排版**：电子表格多向转换、Markdown 渲染至 PDF/EPUB、标准邮件格式（EML/MSG）解析、字体格式转换与子集化提取、数据图表生成。
  - **归档解压**：基于 libarchive-wasm 引入 7z、RAR 及带密码加密 ZIP 文件的纯客户端解压。
  - **端侧 AI**：本地离线 Whisper 语音转写、基于 MI-GAN 与 LaMa 的画面智能消除、面部自动隐私遮蔽、红眼修复、基于显著性检测的智能构图裁切；文字识别（OCR）支持双层可检索 PDF 导出。
- **Pipeline 编排引擎**：支持多表单工具的数据流管道连接，内置多套行业模板，支持编排拓扑的导入导出。
- 集成 Noto Sans SC 离线中文字体资产，彻底解决硬字幕烧录、文字水印及 PDF 生成时的跨平台中文字体缺失问题。

### Plugin API（v2.2 扩展）

- 引入 Tensor 句柄持久化机制：通过 `host.onnx.run(session, feeds, { keep, outputs })` 与 `host.onnx.dispose(tensors)`，支持多步推理间直接复用内存张量，规避重复跨沙盒数据克隆。
- 扩展界面生命周期：新增 `ui.on('canvas', ...)` 事件，精准捕捉视口重绘与画布上下文交接。

### 修复

- 修复高并发线程下静态图像打水印导致 FFmpeg.wasm 挂起的问题：针对单帧静态图像改用确定性单线程解码管线。
- 修复 PDF 解密逻辑，底层全面接入 qpdf 原生解密，杜绝单纯去除权限标识的伪解密行为。

## [0.3.0] - 2026-09-17

### 新增

- 工具总量提升至 57 款，引入端侧流式归档压缩能力（ZIP、TAR、TAR.GZ、GZ）以及第一批端侧视觉模型（AI 智能抠图、超分辨率放大、离线 OCR）。
- 构建 Canvas 与 ImageMagick（Wasm）双渲染驱动架构：兼顾动图多帧完整性、EXIF 与 ICC 色彩空间保留，无缝解码 HEIC、TIFF、PSD、JXL 及 RAW 专业画质格式。
- 落地交互式音视频编辑套件：支持所见即所得的选区裁剪、实时色彩旋转调优预览、动态音量包络线可视化调节及毫秒级帧定位。
- 全面适配移动端视口：采用抽屉式导航架构与响应式顶栏折叠机制。

### Plugin API（v2.1 扩展）

- 支持沙盒动态载入外部资源：开放 `lazy`、`assets` 清单与沙盒内 `loadDependency(id)` 机制。
- 丰富声明式 UI 组件库：新增 `media` 媒体播放器与 `reorder` 拖拽排序节点。
- 产物输出路径标准化，允许写出含多级相对目录的文件结构。

### 变更

- 数据解析引擎全面标准化，底座替换为行业主流解析库（markdown-it、yaml、papaparse、fast-xml-parser）。

### 修复

- 修复高复杂度嵌入图片导致 PDF 在沙盒内 Canvas 渲染上下文崩溃的问题。
- 修复主线程直接调用 ONNX Runtime 导致的 UI 卡顿，隔离至专用后台推理 Worker。
- 修复沙盒严格模式下全局注入依赖未被正确提升的问题。

## [0.2.0] - 2026-09-17

### 新增

- 工具矩阵从初期基础原型扩充至 39 款，覆盖图片处理、PDF 工作流、多媒体转换与文本转码。
- 扩充沙盒宿主核心能力：`ffmpeg`（端侧多媒体转码）、`onnx`（本地神经网络加速）、`secret`（凭据封存与按需签发）。
- 插件 UI 声明式框架 `setup(ui)`：提供响应式组件描述语言与跨上下文的交互画布。
- 深度集成 Monaco Editor 代码编辑器：内置完整的类型系统提示与清单静态审查能力。
- 支持文本级管道直通：直接粘贴文本流，支持计算结果的行内即时展示。

### Plugin API（v2.0 演进）

- 核心机制重构：新增 `setup`、`input`、`textFileName` 字段，新增 `secret`、`onnx` 调用能力，扩充 `fs.remove`、`fs.digest` 操作原语，新增 `archive` 工具分类；保持对 v1.0 插件的向下兼容执行。

### 变更

- 视觉系统全面迁移至官方 shadcn-vue 设计体系。
- 插件轻量存储层 `kv` 升级为端到端 AES-GCM 本地加密存储。
- 全局任务队列移至主界面底部常驻停靠栏。

### 修复

- 修复特定多核心硬件环境下 FFmpeg 多线程调度死锁问题：宿主建立基于 CPU 核心拓扑的动态线程池配额机制。
- 解决隔离沙盒中缺失原生 `crypto.subtle` 的局限：通过宿主代理开放 `host.fs.digest` 哈希计算接口。

## [0.1.0] - 2026-09-16

### 新增

- 核心架构落地：构建基于沙盒化 Worker 与不透明 iframe 的双层隔离执行容器，实现细粒度宿主能力受控调用与虚拟私有文件系统（OPFS）。
- 安全分发机制：支持插件两阶段受控挂载、权限显式披露、以及检测到能力扩展时的更新阻断与复核机制。
- 交互基础框架：响应式三栏工作台、全局 Command Palette（命令面板）、多套系统级主题切换。
- 参考样板插件：高保真图像处理工具套件、PDF 基础处理工具套件。

[Unreleased]: https://github.com/Okysu/OmniTool/compare/v0.5.0...HEAD
[0.5.0]: https://github.com/Okysu/OmniTool/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/Okysu/OmniTool/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/Okysu/OmniTool/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/Okysu/OmniTool/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/Okysu/OmniTool/releases/tag/v0.1.0