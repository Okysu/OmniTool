# 测试场景覆盖 · 第 6 轮

> 面向：本项目的开发者与评审者。

## 运行

```bash
pnpm test          # 单元测试（vitest，422 个）
pnpm test:e2e      # 端到端（Chromium）：素材 → 构建 → 预览服务 → 7 个套件
```

## 本轮新增

### 测试夹具

`tests/harness/plugin.ts` 增加了 `host.net` 与 `host.secret` 的模拟，规则与宿主一致：

- `secret.request` 对已存在的凭据立即返回（除非 `force`），否则由测试脚本扮演用户输入或取消，并记录每次弹窗的参数。
- 请求中的 `{{secret:name}}` 只在凭据存在且目标 origin 在允许列表中时替换，否则请求失败。测试断言看到的是替换后的请求，与模型服务实际收到的一致。
- `run(..., { fetch })` 扮演网络；未提供时请求以「Failed to fetch」失败。每次请求记录在 `result.requests` 中。

### 单元测试

| 文件 | 覆盖 |
| --- | --- |
| `ext-ai-toolkit.test.ts`（34） | **连接**：切换预设填充地址与模型；录入 Key 时绑定接口 origin；保存并测试连接时 Key 以请求头占位符发出；已存 Key 绑定在其他域名时用 `force` 重新请求，用户取消则给出可操作的错误；免 Key 的本机接口不带 `Authorization`；未配置时提示先打开「AI 连接设置」。**服务商差异**：拒绝 `response_format` 时去掉重试；429 按 `Retry-After` 重试；401 转成更换 Key 的提示；宿主拦截的请求不重试；剥离 `<think>`；从说明文字 / 代码块中提取 JSON。**翻译**：代码块不发送给模型、术语表进入系统提示、输出命名；双语对照按段落交替；SRT 批量翻译且时间轴不变；模型合并字幕行时逐条重译；i18n JSON 只译字符串值、键与数字不变。**文本工具**：超长文本分段摘要后汇总；多记录抽取输出 JSON 与带 BOM、正确转义的 CSV；按行分类批量请求并丢弃类别之外的标签；CSV 模板引用不存在的列时报错、结果写入新列；文档问答按向量相似度选段并标注相似度；无向量时关键词打分（含中文二元组）；智能命名清理非法字符并去重。**目标检测**：六种坐标写法（0–1000、像素、Gemini yxyx、0–1、xmin/ymin 颠倒、x/y/width/height）归一化到同一个框；数值大于 1000 时按像素识别；裁剪到画面、丢弃退化框、按类别过滤且大小写不敏感；在 2000×1000 原图、1280 px 发送尺寸下导出 YOLO、COCO、LabelMe 的数值。**视觉**：按类别分文件夹，无效类别归入最后一类；图表转 CSV。**语音与生成**：转写前本机压缩音轨，multipart 请求包含模型、语言与文件名；多段转写合并时校正时间轴；无音轨文件在上传前报错；长文分段合成且每段不超过上限、字节依次拼接；图像生成拒绝 `response_format` 时重试并解码 base64 |
| `analyze.test.ts`（4） | 裸 `fetch(` / `new WebSocket(` 被报告；`host.net.fetch(` 与 `host.net.fetchText(` 不被误报为直接网络调用；`async fetch(` / `function fetch(` 这类方法定义不被误报；`secret`、`onnx`、`ffmpeg` 的使用被识别，只有真正未使用的能力出现在 `unused` 中 |

### 端到端：`extensions` 套件（20）

套件在本机启动一个服务，同时托管仓库的 `extensions/` 目录（带 CORS 头）并模拟 OpenAI 兼容接口。该接口只接受正确的 Bearer Key，并记录每个请求收到的 `Authorization`。

| 检查 | 验证点 |
| --- | --- |
| 订阅与审查 | 通过 `index.json` 订阅；审查对话框显示插件与「外网请求」；不出现「直接网络调用」误报；`secret` 默认未勾选，由测试手动开启；安装成功 |
| 录入凭据 | 运行「保存并测试连接」时宿主弹出凭据对话框，显示的允许域名为接口 origin |
| 本机地址拦截 | 未开启「允许插件访问本机与内网地址」时运行失败并给出宿主的拦截说明，模拟服务器没有收到任何请求 |
| 刷新后持久化 | 开启本机访问并刷新后，已订阅的插件仍在，面板从 `kv` 恢复接口地址，Key 状态显示「已录入（仅发往 …）」。**这一项发现了插件记录未持久化的缺陷** |
| 连接测试 | 摘要显示模型响应正常；服务器收到的 `Authorization` 为替换后的 Key；页面 DOM 中不出现 Key 明文 |
| 翻译 | 长 Markdown 分多个请求发送；结果含译文，代码块未被改动；凭据使用提示合并为一条 |
| 目标检测 | 导出预览图、COCO、YOLO 标签与 `classes.txt`、LabelMe 五个文件；摘要统计正确 |
| 同源依赖 | 表格逐行处理加载 `/vendor/data-libs.js`（Papa）并写入新列；PDF 转 Markdown 在沙盒中通过 pdf.js 渲染页面并发送给视觉模型 |
| 页面错误 | 全程无未捕获异常 |

### 单元测试：`pipelines-store.test.ts`（4）

- 布局清洗：只保留输入、输出与现有步骤的位置，坐标取整；非数值、超出范围、未知键被丢弃。
- 导出再导入：工作流与步骤获得新 id，位置迁移到新步骤 id 上，参数保留。
- 空白与模板工作流没有布局；名称为空时使用「新工作流」。
- 置顶写入 `flow:<id>` 且不重复，与工具置顶共存；删除工作流会取消置顶并清空其会话。

### 端到端：`flows` 套件（23，重写）

| 检查 | 验证点 |
| --- | --- |
| 资料库 | 列表页不显示模板；「新建工作流」对话框中选择模板会填入名称，改名后创建并进入编辑器 |
| 全屏编辑器 | 不出现应用侧边栏与顶栏；名称为输入的名称；列表视图有两个步骤；在运行栏运行 PDF → 图片 → WebP 并得到 WebP 结果 |
| 置顶 | 在编辑器置顶后返回列表，侧边栏出现该工作流；刷新后工作流与置顶都保留；点击侧边栏置顶项打开运行页，页眉列出步骤链，在运行页拖入 PDF 运行成功 |
| 工具页 + 选择器 | 从工具页「加入工作流 → 新建」进入编辑器；选择器不列出面板工具；组成两步工作流并运行成功 |
| 流程图 | 占满编辑器高度（> 700 px）；连线「+」在指定位置插入；检查器删除选中步骤；**拖动节点向左下移动**后位置改变，刷新后仍在原处；「自动排列」让节点回到同一行；切回列表视图内容不变 |
| 页面错误 | 全程无未捕获异常 |

### 单元测试：声谱图与工作区去重

| 文件 | 覆盖 |
| --- | --- |
| `media-tools.test.ts`（声谱图 +2） | FFmpeg 只解码为单声道 s16le、不再使用 `showspectrumpic`，中间 PCM 文件用后删除；3 小时音频请求 44.1 kHz 时降为 16 kHz 并在摘要中说明。合成 1 kHz 正弦（16 kHz 采样）：线性刻度下峰值行误差 ≤ 1 行、幅度约 −6 dBFS、FFT 长度 512；对数刻度下峰值行误差 ≤ 3 行 |
| `vfs-dedupe.test.ts`（5） | 同名同内容再次导入返回同一条目；为第二次选择建立的别名不重复存储、占用空间只算一次，删除原条目后仍可读；异名同内容新建条目并共享存储，删除原条目后别名仍可读，全部删除后清空；与处理结果内容相同的导入共享其存储；大小、首尾 64 KB 相同但中间不同的文件不会被误判为重复 |

插件测试夹具的依赖表增加了 `fft`。

### 端到端：`ui` 套件新增（5，共 19）

| 检查 | 验证点 |
| --- | --- |
| 重复导入复用 | 在两个工具里各拖入一次 `sample-small.png`：第二次提示「已在工作区中，直接复用」，工作区文件数只增加 1 |
| 取消选择不删除 | 在工具的文件列表里移除后，工作区文件数不变 |
| 从工作区选择 | 图片工具的选择器列出该文件且只列出图片；选中后添加并运行成功 |
| 类型过滤 | PDF 工具不会被提供图片文件 |

### 端到端：`tools` 套件新增（4，共 91）

- 声谱图：`tone.m4a` 生成的图片带坐标轴边距（大于 1600×400），60 秒内完成。

### 端到端：`tools` 套件 PDF 渲染检查（3）

沙盒中 pdf.js 渲染的三类回归，素材由 `e2e/fixtures.mjs` 生成，每一项都确认过在修复前失败：

| 检查 | 素材 | 验证点 |
| --- | --- | --- |
| 嵌入字体 | `cjk-embedded.pdf`：用 fonteditor-core 从 Noto Sans SC 裁出 7 个字的字体（pdf-lib 自带的裁剪会丢字形），不再二次裁剪地嵌入 | PDF 转图片的输出与在页面中用同一字体直接绘制的参照逐像素比较，差异须小于与空白页差异的 35%（修复后为 0，修复前比空白页差异还大） |
| CMap | `cjk-cmap.pdf`：STSong-Light，UniGB-UCS2-H 编码，**不嵌入字体** | PDF 提取文本得到「动手学深度学习」，且不再提示「没有文字层」 |
| JPEG 2000 | `jpx.pdf`：整页一张 JPXDecode 红色图片（系统 ffmpeg 编码，缺少编码器时跳过） | PDF 转图片的中心像素为红色（修复前为白色） |

## 手工 / 截图核对

- 扩展 README 中的服务商地址、预设模型名与 `extensions/ai-toolkit.js` 中的预设逐项对照。
- 设置页核对「安全」分组与「允许插件访问本机与内网地址」的文案，与扩展 README 一致。
- 未对真实云端服务或真实 Ollama 做自动化测试（需要 Key 或本机模型）；模拟接口覆盖了协议格式与服务商差异。
- 工作流：1440 宽下的资料库卡片、新建对话框、全屏编辑器（流程图、浮动检查器、拖动后的连线走向）、运行页与侧边栏置顶；400 宽手机视口下资料库页眉按钮换行、卡片单列。
- 订阅安装审查对话框：21 个工具的描述与来源地址不再超出对话框。
- 工作流运行栏（24 rem 宽）中拖放区的格式列表正常换行。
- 「PDF 转图片」渲染嵌入 Noto Sans SC 的中英文页面，与 poppler（`pdftoppm`）的渲染结果对照一致。
- 声谱图：10 分钟 440 Hz 正弦 + 粉红噪声，线性与对数刻度各生成一次，核对音调位置、坐标轴、色标；耗时由 310 秒降至约 1.3 秒。
- 从工作区选择：4 个文件（含 1 个 PDF）导入后在图片工具中打开选择器，只列出 3 张图片，缩略图、选中状态与「添加 1 个」按钮。
